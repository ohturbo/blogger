<h1>All Tags</h1>

<% @tags.each do |tag| %>
	<li><%= link_to tag.name, tag_path(tag), class: "tag" %>
	<%= link_to "delete tag", tag_path(tag), method: :delete, class: "delete"%></li>
<% end %>

<%= link_to "Back to Articles", articles_path%>